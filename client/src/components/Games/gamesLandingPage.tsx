import React from 'react'

function GamesLandingPage() {
    // here i will have corosel of all the games
    // it will have the active players playing the game
    // it will have the top players of the game
    // it will have the top games played by the users
    // it will have the section your invites
    // it will have the section for received invites
    
  return (
    <div>GamesLandingPage</div>
  )
}

export default GamesLandingPage