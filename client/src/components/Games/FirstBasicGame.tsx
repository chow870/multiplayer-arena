import React from 'react'

function FirstBasicGame() {
    // This component will be the first basic game.
    // here i will have to store the socket id of the lobby, user1 and user2.
    // i will have to use socket to transefer data between the two users.
    // i will have to maintain the state that stores the data of the game.
    // i have to maintain the state that handles the turn in the game.
  return (
    <div>FirstBasicGame</div>
  )
}

export default FirstBasicGame