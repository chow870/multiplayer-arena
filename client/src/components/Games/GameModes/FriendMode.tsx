import React from 'react'

function FriendMode() {

    // iss wale mai mujhe we have to just create the invite link
    // vo invite link will be for the waiting lobby actually
    // issme mai event listener hoga : "game_invite created" / "invited" : they can see each other link in the  sections
    // i will create a context for the both the types of invites actually
    // waiting lobby actually mai backend sai get karegi ki kitne log aur kaun kaun aayega
    // jaise hi aapka invite send ho jayega then it will be added in the invited list.
    

  return (
    <div>FriendMode</div>
  )
}

export default FriendMode